import { QuestionTypes } from '../types/quiz'

export const quizTypes = [
  {
    text: 'Use bias definitions as questions',
    type: QuestionTypes.questionDef,
  },
  { text: 'Use bias names as questions', type: QuestionTypes.bias },
]

export const alphabet = ['A', 'B', 'C', 'D']
